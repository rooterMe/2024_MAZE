# 2024_MAZE

This project solves a maze using Reinforcement Learning with a Q-table.

## Requirements

Install `pygame`, `PyQt` before running the program.

> If you don't install `PyQt`, you can run `maze_ai.py`

If you want to change the training parameters, modify the **maze_ai** section in `program.py`.
